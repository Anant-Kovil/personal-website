import { Typography } from "@mui/material"

export const Error = () => {
  return (
    <Typography variant="h6" sx={{ fontWeight: 300, display: 'flex', alignSelf:'center', justifyContent: 'center'}}>Sorry you have hit the wrong url</Typography>
  )
}